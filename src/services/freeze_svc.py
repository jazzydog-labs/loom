class FreezeSvc:
    """Service orchestrating freeze creation and checkout."""

    def create_freeze(self, tag: str) -> str:
        return f"TODO: create freeze for {tag}"

    def checkout(self, freeze_id: str) -> str:
        return f"TODO: checkout freeze {freeze_id}"
